# toolbox_F.conf (external/public)
PROFILE="F"

# modules enabled
ENABLE_NETCHECK="1"
ENABLE_DISK_HEALTH="1"

# 禁止任何备份/同步能力
ENABLE_BACKUP="0"
ENABLE_SYNC="0"

# 报告输出（你可以保留你自己的目录，也可以改成更通用）
REPORT_DIR="${REPORT_DIR:-$HOME/toolbox/IT-Reports}"
LOG_DIR="${LOG_DIR:-$HOME/toolbox/Logs}"
