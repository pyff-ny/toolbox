# conf/crawler.conf  (zsh)

# 项目根目录（toolbox 选择 4 会 cd 到这里）
CRAWL_PROJECT_ROOT="$HOME/toolbox/ops/novel"

# venv 激活脚本（按你实际 .venv 路径改）
CRAWL_VENV_ACTIVATE="$CRAWL_PROJECT_ROOT/.venv/bin/activate"

# Python 脚本文件名（在项目根目录下）
CRAWL_SCRIPT="novel_crawler.py"

# 必填：目录页 URL（toc_url）
CRAWL_TOC_URL="https://www.bidutuijian.com/books/yztpingsanguo/000.html"

# 输出目录（默认）
CRAWL_OUT="./yztpingsanguo"

# 合并输出文件（可选；留空表示不 merge）
CRAWL_MERGE="易中天品三国.md"

# 可选：范围
CRAWL_START=""   # 例如 1
CRAWL_END=""     # 例如 50

# 可选：睡眠与超时
CRAWL_MIN_SLEEP="1.5"
CRAWL_MAX_SLEEP="3.0"
CRAWL_TIMEOUT="20"

# 可选：是否忽略 robots（不建议；留 0）
CRAWL_IGNORE_ROBOTS=0
