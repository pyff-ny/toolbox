```bash

cat > ~/Ops/disk-health/conf/device_types.txt <<'EOF'
# 依次尝试的 smartctl -d 类型（按常见程度排序）
# 空行和 # 注释会被忽略
sat
sntasmedia
usbjmicron
EOF
## 说明：不同外置盒/桥接芯片支持不同，
##/三星 T7 这类 USB SSD 可能全部都不透传，脚本会给出“读不到 SMART 的结论”，不是你做错。